import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import Aviasales from './containers/Aviasales';

const domContainer = document.getElementById('root');
ReactDOM.render(<Aviasales />, domContainer);
